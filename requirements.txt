# Sekisho Sync Suite — Python dependencies
# Pinned for reproducibility. All packages installed into:
#   /opt/sekisho-sync/venv
#
# To update: test new versions manually, then update pins here.
# Do NOT use pip install without updating this file.
#
# pip itself — pinned for full determinism
pip==24.3.1

# Direct dependency:
flask==3.0.3

# Flask transitive dependencies (pinned for full reproducibility):
werkzeug==3.0.6
jinja2==3.1.6
click==8.1.8
itsdangerous==2.2.0
markupsafe==3.0.2
blinker==1.9.0
